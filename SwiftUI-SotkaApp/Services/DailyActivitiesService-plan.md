# План реализации сервиса DailyActivitiesService

## Статус: ✅ Выполнено

### Основная функциональность ✅
Класс `DailyActivitiesService` с методами для работы с активностями: `createDailyActivity()`, `markDailyActivityAsModified()`, `deleteDailyActivity()`, `syncDailyActivities()`, `set()`. Двунаправленная синхронизация с snapshots, офлайн-приоритет, мягкое удаление через `shouldDelete`. Интеграция в `StatusManager.syncJournalAndProgress()`. 34 теста (22 основной функциональности + 12 для `set`).

### Метод set ✅
Метод `set()` реализован и протестирован. Обрабатывает типы `stretch`, `rest`, `sick` (для `workout` логирует). Создает или обновляет активность с очисткой тренировочных данных.

### UI интеграция ✅
**HomeActivitySectionView**: подключен к `activitiesService` для установки/удаления активностей. Использует `DayActivityMenuView` и `DayActivityContentView`. Анимация переходов, `confirmationDialog` для удаления. **JournalListView/GridView**: действия подключены к сервису. **User.activitiesByDay**: добавлен фильтр `!shouldDelete` для исключения удаленных активностей и тест.

### План реализации ✅

**Этап 0**: Созданы 4 переиспользуемых компонента (`DayActivityHeaderMenuView`, `DayActivityMenuView`, `DayActivityTrainingView`, `DayActivityContentView`). `JournalListView` обновлен.

**Этапы 1-8**: `HomeActivitySectionView` интегрирован с `activitiesService`:
- `@Query` для активностей, `currentActivity` с фильтром `!shouldDelete`
- `HomeSectionTitleView` с условным меню, `DayActivityContentView` и кнопки выбора
- `DayActivityMenuView` подключен к `activitiesService.set()` и `deleteDailyActivity()`
- `confirmationDialog` для удаления, анимация переходов
- Все критерии приемки выполнены

## Примечания

1. **Офлайн-приоритет**: Все операции сначала выполняются локально, синхронизация опциональна
2. **Безопасность**: Никаких force unwrap, только безопасное извлечение опционалов
3. **Производительность**: Конкурентные задачи для параллельной синхронизации (snapshots)
4. **Консистентность**: Следование паттернам из `CustomExercisesService`
5. **Вычисляемые свойства**: Преобразования данных через вычисляемые свойства в моделях

---

## Анализ функционала комментариев к активностям

### Статус: 📋 Анализ завершен, готов к реализации

### Анализ реализации в Android-приложении

#### Хранение и синхронизация
- **Модель**: `Day.comment: String = ""` (не опциональное, но может быть пустой строкой)
- **Локальное хранение**: Комментарий сохраняется в SQLite таблице `TABLE_DAY` в поле `comment` (тип `text`)
- **Синхронизация**: Комментарий отправляется на сервер через `UploadTrainingDayCommandImpl`:
  ```kotlin
  queryMap["comment"] = day.comment
  ```
- **Проблема**: Пользователь сообщил, что комментарий сохраняется локально, но не синхронизируется с сервером. Причина не установлена, но возможно связано с логикой синхронизации или валидацией на сервере.

#### UI функционал
- **Добавление комментария**: Диалог `dialog_comment.xml` с `EditText` для ввода
- **Редактирование комментария**: Тот же диалог, заполненный текущим значением
- **Удаление комментария**: Установка пустой строки `day.comment = ""`
- **Отображение**: Комментарий показывается в списке дней, если он не пустой
- **Места использования**: 
  - `TrainingsFragment` - основной экран тренировок
  - `TrainingsPreviewFragment` - экран предпросмотра тренировки

### Анализ реализации на сервере (StreetWorkoutSU)

#### API поддержка
- **Endpoint**: `POST v3/100/days/<day>` (обновление) и `POST v3/100/days` (создание)
- **Контроллер**: `StoAppController::editDayTraining()` → `ApiStoDayTrainingForm::updateDay()`
- **Модель**: `StoDay.comment` (тип `string` в БД)

#### Валидация и ограничения
- **Тип**: `string` (строка)
- **Ограничения**: 
  - В правилах валидации `ApiStoDayTrainingForm`: `['comment', 'string']` - без ограничений по длине
  - Поле опциональное (может быть `null` или пустой строкой)
- **Сохранение**: Комментарий сохраняется вместе с остальными полями активности через `$sto_day->comment = $this->comment`

#### Синхронизация
- **При получении**: Комментарий возвращается в ответе `ApiStoDay` через поле `comment`
- **При отправке**: Комментарий принимается в теле запроса и сохраняется в БД
- **Логика обновления**: Комментарий обновляется только если `modifyDate` клиента новее серверного (стандартная логика синхронизации)

### Текущее состояние в новом iOS-приложении

#### Модели данных ✅
- **DayActivity**: Поле `comment: String?` присутствует
- **DayRequest**: Поле `comment: String?` включено в запрос, отправляется через `formParameters["comment"]`
- **DayResponse**: Поле `comment: String?` присутствует в ответе сервера
- **ActivitySnapshot**: Поле `comment: String?` включено для синхронизации

#### Синхронизация ✅
- **Отправка**: Комментарий включается в `DayRequest` через `dayRequest` computed property
- **Получение**: Комментарий извлекается из `DayResponse` при синхронизации
- **Обновление**: Комментарий обновляется в `DailyActivitiesService.syncUnsyncedActivities()` и `downloadServerActivities()`

#### Проблемы и ограничения ⚠️
1. **Нет UI для редактирования**: Комментарий не отображается и не редактируется в интерфейсе
2. **Нет методов для работы с комментариями**: В `DailyActivitiesService` нет специализированных методов для установки/обновления/удаления комментариев

### Выводы и рекомендации

#### ✅ Функционал на сервере готов
- Серверная часть полностью поддерживает комментарии
- API endpoints работают корректно
- Нет ограничений по длине комментария
- Комментарий синхронизируется вместе с остальными данными активности

#### ✅ Базовая инфраструктура в приложении готова
- Модели содержат поле `comment`
- Синхронизация работает (комментарий отправляется и получается)
- Осталось только добавить UI и методы для работы с комментариями

#### ⚠️ Требуется реализация
1. **Методы в DailyActivitiesService**:
   - `updateComment(day: Int, comment: String?, context: ModelContext)` - обновление комментария
   - Метод должен работать по принципу офлайн-приоритета (сначала локально, потом синхронизация)
   
2. **UI компоненты**:
   - Отображение комментария в `JournalListView` и `JournalGridView`
   - Диалог/модальное окно для редактирования комментария (аналогично Android)
   - Кнопка "Добавить комментарий" / "Редактировать комментарий"
   - Кнопка "Удалить комментарий" (установка `nil`)

### План реализации с учетом TDD

#### Этап 1: Методы сервиса (TDD) ✅
1. ✅ **Написать тесты** для `updateComment()`: 6 тестов покрывают создание, обновление, удаление комментария, проверку флагов синхронизации и обновление даты.
   
2. ✅ **Реализовать метод** `updateComment()` в `DailyActivitiesService`: метод реализует офлайн-приоритет (локальное сохранение), обновляет флаги синхронизации и поддерживает создание активности для несуществующего дня.

3. ✅ **Запустить тесты**: все 39 тестов проходят, код отформатирован.

#### Этап 2: UI компоненты
1. **Создать компонент** `DayActivityCommentView`:
   - Отображение комментария (если есть)
   - Кнопка "Добавить комментарий" / "Редактировать комментарий"
   - Кнопка "Удалить комментарий" (если комментарий есть)

2. **Создать модальное окно** `EditCommentSheet` (по аналогии с `SendMessageScreen` из WorkoutApp):
   - **Анализ требований**:
     - ✅ Активность **ДОЛЖНА существовать** для редактирования комментария (нельзя создать активность только с комментарием без типа активности)
     - ✅ На сервере все поля опциональны, но для установки прохождения дня требуется `activity_type`
     - ✅ В Android-приложении комментарий редактируется для существующей активности
     - ✅ Можно передать `DayActivity` напрямую во вьюху (без `@Query`)
   
   - **Структура компонента**:
     - `@Bindable var activity: DayActivity` - активность для редактирования комментария (не опциональная, так как активность должна существовать)
       - `@Bindable` позволяет напрямую создавать binding к свойствам SwiftData модели
     - `@FocusState private var isFocused` - состояние фокуса для автоматического открытия клавиатуры
     - `@Environment(\.activitiesService) private var activitiesService` - сервис для операций с активностями (обновление флагов синхронизации)
     - `@Environment(\.modelContext) private var modelContext` - контекст SwiftData для сохранения
     - `@Environment(\.dismiss) private var dismiss` - закрытие модального окна
     - Computed property для binding к `comment`:
       - `var commentBinding: Binding<String>` - преобразует `activity.comment` (String?) в `Binding<String>` для `SWTextEditor`
       - При get: возвращает `activity.comment ?? ""`
       - При set: устанавливает `activity.comment = newValue.isEmpty ? nil : newValue`
       - Это позволяет напрямую редактировать `comment` без промежуточного `@State`
   
   - **Логика работы с данными**:
     - При сохранении (кнопка "Сохранить"):
       - Установить `isFocused = false`
       - Вызвать `activitiesService.updateComment(day: activity.day, comment: activity.comment, context: modelContext)`
       - Закрыть модальное окно через `dismiss()`
     - Комментарий может быть пустым (для удаления комментария - передать `nil`)
     - Изменения в `activity.comment` через binding автоматически сохраняются в модели (SwiftData отслеживает изменения)
   
   - **UI структура**:
     - Использовать `ContentInSheet` с заголовком:
       - Title: `"DayActivity.Comment.Title"` (русский: "Комментарий")
     - `VStack` с:
       - `SWTextEditor` для многострочного ввода:
         - `text: commentBinding` - привязка к `activity.comment` через computed binding
         - `placeholder: "Опишите этот день…"` - плейсхолдер
         - `isFocused: isFocused` - состояние фокуса
         - `height: 200` - фиксированная высота
         - `.focused($isFocused)` - модификатор фокуса
         - `.task` для автоматического открытия клавиатуры через 750ms после появления
       - `Spacer()` - для отступа
       - Кнопка "Сохранить":
         - Текст: `"DayActivity.Comment.Save"` (русский: "Сохранить")
         - Стиль: `SWButtonStyle(mode: .filled, size: .large)`
         - При нажатии: логика сохранения (см. выше)
   
   - **Отличия от `SendMessageScreen`**:
     - ❌ Нет `isLoading` - не показываем индикатор загрузки (offline-first)
     - ❌ Нет `isSendButtonDisabled` - не блокируем кнопку (offline-first)
     - ❌ Нет проверки соединения с сетью - не используем `@Environment(\.isNetworkConnected)`
     - ❌ Нет `.loadingOverlay()` и `.interactiveDismissDisabled()` модификаторов
     - ✅ Комментарий может быть пустым (для удаления комментария)
     - ✅ Логика работы с данными внутри компонента (не через binding и action)
   
   - **Инициализатор**:
     ```swift
     init(activity: DayActivity)
     ```
     - Принимает `activity: DayActivity` (не опциональный, так как активность должна существовать для редактирования комментария)
     - Использует `@Bindable var activity` для прямого доступа к свойствам модели
     - Не требует инициализации локального состояния, так как используется computed binding

3. **Интегрировать в экраны**:
   - `JournalListView`: добавить `DayActivityCommentView` в ячейку
   - `JournalGridView`: добавить `DayActivityCommentView` в карточку
   - В `DayActivityMenuView.onComment`: открывать `EditCommentSheet` только если `activity != nil`
   - Передавать существующую `activity` в `EditCommentSheet(activity: activity)`
   - Подключить к `activitiesService.updateComment()`

#### Этап 3: Исправление логики
1. ✅ **Метод `DayActivity.setNonWorkoutType()` правильный**:
   - Метод корректно очищает все поля (включая комментарий) при изменении типа активности на `stretch`/`rest`/`sick`
   - Пересматривать метод не требуется

### Требования и ограничения

#### Серверные требования
- ✅ Комментарий - строка без ограничений по длине
- ✅ Комментарий опционален (может быть `null` или пустой строкой)
- ✅ Комментарий синхронизируется вместе с активностью
- ✅ Удаление комментария = отправка `null` или пустой строки

#### Клиентские требования
- ✅ Офлайн-приоритет: комментарий сохраняется локально, синхронизация опциональна
- ✅ Безопасное извлечение опционалов (никаких force unwrap)
- ✅ Логирование через `OSLog`
- ✅ Следование паттернам из существующих сервисов

#### UI требования (на основе Android)
- Комментарий отображается только если он не пустой
- Кнопка "Добавить комментарий" показывается если комментария нет
- Кнопка "Редактировать комментарий" показывается если комментарий есть
- Кнопка "Удалить комментарий" показывается если комментарий есть
- Диалог подтверждения при удалении комментария (опционально)

### Критерии приемки

1. ✅ **Функционал на сервере готов** - проверено
2. ✅ **Методы в сервисе** - `updateComment()` реализован и протестирован (6 тестов)
3. ⏳ **UI компоненты** - комментарий отображается и редактируется
4. ⏳ **Синхронизация** - комментарий корректно синхронизируется с сервером
5. ✅ **Тесты** - все 39 тестов проходят
6. ✅ **Форматирование** - код отформатирован
