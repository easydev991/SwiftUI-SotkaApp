# Экран прогресса (Progress)

Этот документ описывает реализованный функционал экрана прогресса, устройство локального хранения, офлайн-приоритет и логику сервиса `ProgressService.swift`.

### Краткий обзор функционала

- **Сетка прогресса**: отображение результатов по контрольным точкам (день 1, 49, 100) с группировкой по типам упражнений.
- **Редактирование**: форма ввода результатов (подтягивания, отжимания, приседания, вес) с валидацией.
- **Создание/обновление**: локальное сохранение в SwiftData, последующая синхронизация в фоне.
- **Удаление**: мягкое удаление (soft delete) — элемент помечается флагом, сразу скрывается из UI.
- **Навигация**: переходы сетка → редактирование с сохранением состояния.
- **Интеграция с главным экраном**: секция "Заполнить результаты" появляется при отсутствии активного прогресса.

### Модель данных: `Progress`

- `id: Int` — номер дня (1, 49, 100) — совпадает с контрольными точками программы.
- `pullUps: Int?` — количество подтягиваний.
- `pushUps: Int?` — количество отжиманий.
- `squats: Int?` — количество приседаний.
- `weight: Float?` — вес пользователя.
- `isSynced: Bool` — синхронизировано ли состояние с сервером.
- `shouldDelete: Bool` — пометка на удаление (элемент скрывается из UI).
- `lastModified: Date` — дата последнего изменения (используется для разрешения конфликтов).
- `user: User?` — связь с пользователем через `@Relationship`.

### Офлайн-приоритет

- Все операции (создание, обновление, удаление) сначала сохраняются локально в SwiftData.
- Синхронизация с сервером выполняется асинхронно, неблокирующе для обычных пользовательских действий.
- При сетевых ошибках работа продолжается локально; состояние помечается как несинхронизированное.
- Конфликты разрешаются автоматически по стратегии LWW (Last Write Wins) на основе поля `lastModified`.

### Сервис: `ProgressService`

Сервис помечен `@MainActor` и `@Observable`. Все логи — на русском языке (`OSLog`).

Основные методы сервиса:
- Создание и редактирование прогресса с валидацией данных
- Мягкое удаление записей прогресса
- Синхронизация данных с сервером через `ProgressSyncService`
- Управление состоянием формы редактирования

### Сетевая интеграция

- **ProgressClient** в `SWClient` с методами API для работы с прогрессом
- Модели `ProgressRequest` и `ProgressResponse` для обмена данными
- Двунаправленная синхронизация через `ProgressSyncService`
- Разрешение конфликтов по LWW стратегии

### UI: взаимодействие и поведение

- **Сетка прогресса**: отображение результатов по контрольным точкам с фильтрацией удаленных записей.
- **Форма редактирования**: поля ввода с валидацией и кнопка сохранения.
- **Кнопка удаления**: отображается только для записей с данными, подтверждение через диалог.
- **Навигация**: переходы сетка → редактирование с сохранением состояния.

### Интеграция с главным экраном

- **HomeFillProgressSectionView**: секция на главном экране
  - Показывается, если `!user.isMaximumsFilled(for: currentDay)`.
  - Ссылается на экран прогресса для заполнения данных.

### Тестирование и качество

- Unit-тесты для `ProgressService` — проверка создания, обновления, удаления, валидации.
- Проверки локализации пользовательских строк и корректности логов (на русском).
- Тестирование интеграции с главным экраном.

### Принципы расширения

- Новые поля в `Progress` добавлять с учетом мягких миграций SwiftData.
- Для новых операций соблюдать офлайн-приоритет: локальные изменения в первую очередь, синхронизация — отдельно и неблокирующе.
- Все сетевые операции — через абстракции клиентов, без прямых вызовов внутри View.

### Ключевые правила

- Не блокировать UI ожиданием сети в экране прогресса.
- Всегда писать логи на русском с достаточным контекстом.
- Разрешать конфликты автоматически по `lastModified`.
- Soft delete: сначала пометка и скрытие в UI, потом удаление на сервере и физическое удаление локально.


        applySyncEvents(eventsById, context: context)
        
        logger.info("Синхронизация прогресса завершена")
    } catch {
        logger.error("Ошибка получения несинхронизированного прогресса: \(error.localizedDescription)")
    }
}

/// Формирует список снимков локального прогресса, требующих синхронизации
private func makeProgressSnapshotsForSync(context: ModelContext) throws -> [ProgressSnapshot] {
    /// Берем все несинхронизированные, а также те, что помечены на удаление
    let toSync = try context.fetch(
        FetchDescriptor<Progress>(
            predicate: #Predicate { !$0.isSynced || $0.shouldDelete }
        )
    )
    let snapshots: [ProgressSnapshot] = toSync.map { progress in
        ProgressSnapshot(
            id: progress.id,
            pullups: progress.pullUps,
            pushups: progress.pushUps,
            squats: progress.squats,
            weight: progress.weight,
            lastModified: progress.lastModified,
            isSynced: progress.isSynced,
            shouldDelete: progress.shouldDelete,
            userId: progress.user?.id
        )
    }
    return snapshots
}

/// Выполняет конкурентные сетевые операции синхронизации и собирает результаты без доступа к `ModelContext`
private func runSyncTasks(
    snapshots: [ProgressSnapshot],
    client: ProgressClient
) async -> [Int: SyncEvent] {
    await withTaskGroup(of: (Int, SyncEvent).self) { group in
        for snapshot in snapshots {
            logger.info("Отправляем прогресс на сервер: день \(snapshot.id)")
            
            group.addTask { [snapshot] in
                let event = await self.performNetworkSync(for: snapshot, client: client)
                return (snapshot.id, event)
            }
        }
        
        var eventsById: [Int: SyncEvent] = [:]
        for await (id, event) in group {
            eventsById[id] = event
        }
        return eventsById
    }
}

/// Выполняет сетевую синхронизацию одного снимка без доступа к `ModelContext`
private func performNetworkSync(
    for snapshot: ProgressSnapshot,
    client: ProgressClient
) async -> SyncEvent {
    do {
        if snapshot.shouldDelete {
            try await client.deleteProgress(day: snapshot.id)
            return .deleted(id: snapshot.id)
        } else {
            let request = ProgressRequest(
                id: snapshot.id,
                pullups: snapshot.pullups,
                pushups: snapshot.pushups,
                squats: snapshot.squats,
                weight: snapshot.weight,
                modifyDate: snapshot.lastModified.ISO8601Format()
            )
            
            /// Определяем, создание или обновление
            let response: ProgressResponse
            if snapshot.isSynced {
                /// Обновляем существующий прогресс
                response = try await client.updateProgress(day: snapshot.id, progress: request)
            } else {
                /// Создаем новый прогресс
                response = try await client.createProgress(progress: request)
            }
            
            return .createdOrUpdated(id: snapshot.id, server: response)
        }
    } catch {
        return .failed(id: snapshot.id, errorDescription: error.localizedDescription)
    }
}

/// Применяет результаты синхронизации к локальному хранилищу в одном месте
private func applySyncEvents(_ events: [Int: SyncEvent], context: ModelContext) {
    do {
        /// Загружаем текущего пользователя и все записи прогресса заранее
        guard let user = try context.fetch(FetchDescriptor<User>()).first else {
            logger.error("Пользователь не найден при применении результатов синхронизации")
            return
        }
        let existing = try context.fetch(FetchDescriptor<Progress>()).filter { $0.user?.id == user.id }
        let dict = Dictionary(uniqueKeysWithValues: existing.map { ($0.id, $0) })
        
        for (id, event) in events {
            switch event {
            case let .createdOrUpdated(_, server):
                if let local = dict[id] {
                    /// LWW — берем серверную модификацию, так как она возвращается с актуальной датой
                    updateLocalFromServer(local, server)
                    logger.info("Обновлено локально прогресс дня \(local.id) по данным сервера")
                } else {
                    /// Создаем новую запись локально по ответу сервера
                    let newProgress = Progress(from: server, user: user)
                    context.insert(newProgress)
                    logger.info("Создано локально прогресс дня \(newProgress.id) из ответа сервера")
                }
            case .deleted:
                if let local = dict[id] {
                    context.delete(local)
                    logger.info("Удалено локально прогресс дня \(id)")
                } else {
                    logger.debug("Удаление: локальный прогресс дня \(id) не найден")
                }
            case let .failed(_, errorDescription):
                logger.error("Ошибка синхронизации прогресса дня \(id): \(errorDescription)")
            }
        }
        
        try context.save()
    } catch {
        logger.error("Ошибка применения результатов синхронизации: \(error.localizedDescription)")
    }
}

/// Обновляет локальный прогресс данными с сервера
private func updateLocalFromServer(_ local: Progress, _ server: ProgressResponse) {
    local.pullUps = server.pullups
    local.pushUps = server.pushups
    local.squats = server.squats
    local.weight = server.weight
    local.lastModified = DateFormatterService.dateFromString(server.modifyDate, format: .serverDateTimeSec)
    local.isSynced = true
    local.shouldDelete = false
}

/// Методы для работы с синхронизацией в модели Progress
extension Progress {
    /// Обновляет URL фотографий из ответа сервера
    func updatePhotoUrls(from response: ProgressResponse) {
        /// Обновляем URL фотографий, если они есть в ответе
        /// (логика будет реализована во второй итерации с фотографиями)
    }
    
    /// Отмечает прогресс как синхронизированный
    func markAsSynced() {
        isSynced = true
        shouldDelete = false
    }
    
    /// Проверяет, является ли запись новой (не синхронизированной)
    var isNew: Bool {
        !isSynced && !shouldDelete
    }
    
    /// Создает Progress из ProgressResponse
    convenience init(from response: ProgressResponse, user: User) {
        self.init(
            id: response.id,
            pullUps: response.pullups,
            pushUps: response.pushups,
            squats: response.squats,
            weight: response.weight
        )
        self.user = user
        self.lastModified = DateFormatterService.dateFromString(response.modifyDate, format: .serverDateTimeSec)
        self.isSynced = true
        self.shouldDelete = false
    }
}

/// Скачивание обновленного прогресса с сервера
private func downloadServerProgress(context: ModelContext) async throws {
    /// Логика скачивания и обновления данных прогресса
    /// с автоматическим разрешением конфликтов по LWW
}

/// Удаление помеченных фотографий на сервере
private func deleteMarkedPhotos(context: ModelContext) async throws {
    let photosToDelete = photos.filter { $0.needsDelete }
    
    for photo in photosToDelete {
        try await client.deleteProgressPhoto(day: id, photoType: photo.type.rawValue)
    }
}
```

#### Обработка изображений

##### `ImageProcessor` сервис

```swift
struct ImageProcessor {
    static let maxImageSize: CGFloat = 1024
    static let compressionQuality: CGFloat = 0.8
    
    // Сжатие и масштабирование изображения
    static func processImage(_ image: UIImage) -> Data?
    
    // Создание thumbnail'а для предварительного просмотра
    static func createThumbnail(from image: UIImage, size: CGSize) -> UIImage?
    
    // Валидация размера файла
    static func validateImageSize(_ data: Data) -> Bool
}
```

#### UI интеграция

##### Расширение `EditProgressScreen`

- Добавить секцию с фотографиями под полями ввода данных
- `ProgressPhotoGrid` для отображения текущих фотографий
- Кнопки "Добавить фото" для каждого типа
- Индикаторы синхронизации для каждой фотографии

##### Навигация

- Модальный переход к `ProgressPhotoPicker` при выборе фотографии
- Полноэкранный просмотр фотографий с возможностью удаления
- Обязательное подтверждение удаления через диалог (без возможности восстановления)

#### Принципы реализации

##### Офлайн-приоритет для фотографий

- Все фотографии сохраняются локально в SwiftData как `Data`
- Синхронизация выполняется асинхронно в фоне
- При сетевых ошибках работа продолжается локально
- Конфликты разрешаются по стратегии LWW

##### Управление памятью

- Использование `Data` для хранения в SwiftData (автоматическое управление памятью)
- Создание thumbnail'ов для UI (меньший размер в памяти)
- Очистка временных файлов после обработки

##### Безопасность и приватность

- Запрос разрешений на доступ к фотографиям
- Валидация типов файлов (только изображения)
- Ограничение размера загружаемых файлов

## Анализ серверной логики и сравнение с другими приложениями

### Анализ серверного кода (StreetWorkoutSU)

#### Почему сервер возвращает ошибку "Уже имеется запись для этого дня пользователя"

**Корень проблемы:** В модели `StoProgress` на сервере есть правило валидации:

```php
[['day'], 'unique', 'targetAttribute' => ['user_id', 'day'], 'message' => 'Уже имеется запись для этого дня пользователя']
```

Это правило говорит Yii, что комбинация `user_id` + `day` должна быть уникальной в таблице `wo_sto_progress`. Когда клиент пытается создать новую запись с уже существующей комбинацией `user_id` + `day`, Yii выбрасывает ошибку валидации с сообщением "Уже имеется запись для этого дня пользователя".

**Логика сервера в ApiStoProgressForm.php:**
- Метод `updateProgress()` получает `modify_date` от клиента
- Сравнивает его с `modified_at` в базе данных
- Если дата клиента новее → обновляет данные
- Если дата клиента старше → игнорирует обновление и возвращает `null`
- Если записи нет в базе → создает новую

**Проблема в текущей реализации:** Наш SwiftUI клиент всегда пытается создать новую запись через `createProgress()`, даже если запись уже существует на сервере.

### Сравнение с другими приложениями

#### Android приложение (Android-SOTKA)

**Стратегия синхронизации прогресса:**

1. **Двойная попытка отправки:**
   ```kotlin
   // В ProgressInteractorImpl:
   syncInteractor.uploadProgress(progress = progress, PHOTO_ALL)
       .doOnError {
           // Если не получилось upload, значит такая запись уже есть
           syncInteractor.modifyProgress(progress = progress, PHOTO_ALL)
               .subscribe()
       }
       .subscribe()
   ```

2. **Два разных метода:**
   - `uploadProgress()` - создание новой записи (endpoint без номера дня)
   - `modifyProgress()` - обновление существующей записи (endpoint с номером дня)

3. **LWW логика на клиенте:**
   - При загрузке данных с сервера сравнивает локальные и серверные даты
   - Тот, у кого новее дата модификации - выигрывает

#### Старое iOS приложение (SOTKA-OBJc)

**Стратегия синхронизации:**

1. **Последовательная синхронизация по одной записи**
2. **Метод `upload` в DbUser:**
   - Пытается отправить данные на сервер
   - При ошибке дубликата просто помечает запись как синхронизированную локально
   - Не пытается разрешать конфликты на основе дат

3. **Простая логика:**
   - Если сервер принимает данные → помечает как синхронизированное
   - Если сервер отвергает (дубликат) → тоже помечает как синхронизированное

### Рекомендации для исправления

#### Проблема текущей реализации

Наша текущая логика в `ProgressSyncService` имеет следующие проблемы:

1. **Хардкод ошибки:** Проверяем конкретный текст ошибки вместо логики LWW
2. **Фиктивные ответы:** Создаем искусственные ответы сервера вместо правильного разрешения конфликтов
3. **Неправильная стратегия:** Всегда пытаемся создать новую запись вместо попытки обновления

#### Рекомендуемое решение

1. **Убрать хардкод** и реализовать чистую LWW логику
2. **Использовать двойную попытку:** Сначала `upload`, при ошибке дубликата - `modify`
3. **Правильно обрабатывать ответы сервера** на основе реальных дат модификации

#### Упрощенная архитектура (после рефакторинга)

```swift
// В ProgressSyncService:
enum SyncResult {
    case uploaded(ProgressResponse)     // Успешно обновлено/создано через единый updateProgress
    case failed(String)                 // Ошибка синхронизации
}

// Упрощенная логика:
// 1. Всегда используем updateProgress (сервер сам разберется)
// 2. Сервер применяет LWW логику по датам модификации
// 3. При успехе - применяем LWW логику сравнения дат локально
// 4. Нет необходимости в специальной обработке дубликатов
```

#### Последовательность реализации

##### Первая итерация (без фотографий):
1. ✅ **Создать `ProgressClient`** в `SWClient` с методами API
2. ✅ **Добавить модели** `ProgressRequest` и `ProgressResponse` (фотографии = nil)
3. ✅ **Расширить `ProgressService`** методами синхронизации данных
4. ✅ **Реализовать snapshot/group/apply** схему для синхронизации
5. ✅ **Добавить LWW конфликт-резолюшн** по `lastModified`
6. ✅ **Интегрировать синхронизацию** в UI (опционально — кнопка синхронизации)
7. ✅ **Написать тесты** для синхронизации с моками клиента
8. ✅ **Протестировать** все сценарии синхронизации данных

##### Вторая итерация (исправление логики синхронизации):
9. **Реализовать двойную попытку отправки:** Сначала `upload`, при ошибке дубликата - `modify`
10. **Убрать хардкод** проверки текста ошибки в пользу чистой LWW логики
11. **Исправить обработку ответов сервера** на основе реальных дат модификации
12. **Протестировать исправленную логику** с различными сценариями конфликтов

##### Третья итерация (с фотографиями):
13. **Расширить модель `Progress`** полями для фотографий
14. **Создать `ImageProcessor`** для обработки изображений
15. **Реализовать `ProgressPhotoView`** и связанные UI компоненты
16. **Расширить `ProgressService`** методами работы с фотографиями
17. **Обновить `ProgressRequest`** для поддержки фотографий
18. **Интегрировать в `EditProgressScreen`** секцию с фотографиями
19. **Реализовать синхронизацию фотографий** в фоновом режиме
20. **Добавить тесты** для всех компонентов работы с фотографиями


## TODO

### 🔧 Критические исправления логики прогресса

1. **Исправить модель `Progress.swift`**:
   - Изменить `case two = 50` на `case two = 49`
   - Изменить логику `init(day: Int)`: `case 49 ... 99: .two`
   - Обновить маппинг в `getExternalDayFromProgressId` и `getInternalDayFromExternalDay`

2. **Обновить документацию и комментарии**:
   - Изменить все ссылки на "день 50" на "день 49"
   - Обновить описания в планах реализации
   - Обновить комментарии в коде

3. **Обновить тесты и превью**:
   - Изменить тестовые данные для соответствия новым дням
   - Обновить превью в `ProgressGridView` для демонстрации правильной логики
   - Обновить комментарии в тестах

4. **Протестировать исправления**:
   - Проверить доступность прогресса на разных днях программы
   - Убедиться, что синхронизация работает с новыми днями
   - Тестировать UI на корректность отображения

### ✅ Выполненные исправления синхронизации прогресса

1. ✅ **Упростить логику синхронизации** в `ProgressSyncService`:
   - ✅ Убрать двойную попытку отправки (upload + modify)
   - ✅ Использовать единый `updateProgress` для всех случаев
   - ✅ Сервер сам применяет LWW логику по датам модификации

2. ✅ **Улучшить обработку ошибок сервера**:
   - ✅ Убрать хардкод проверки текста ошибки
   - ✅ Использовать универсальную логику на основе кодов ответа HTTP
   - ✅ Упростить обработку дубликатов

3. ✅ **Протестировать исправленную логику**:
   - ✅ Сценарий: локальные изменения → синхронизация → серверные данные новее
   - ✅ Сценарий: локальные изменения → синхронизация → локальные данные новее
   - ✅ Сценарий: одновременные изменения на разных устройствах

### Рефакторинг и упрощение

**Рекомендуется использовать Вариант 1** (разделение на мелкие методы) по следующим причинам:

1. **Улучшает читаемость** - каждый метод выполняет одну задачу
2. **Повышает тестируемость** - можно тестировать каждый метод отдельно
3. **Облегчает поддержку** - легче находить и исправлять баги
4. **Сохраняет существующую логику** - не меняет поведение приложения
5. **Лучше документирует процесс** - каждый метод имеет четкую ответственность

**Дополнительные улучшения:**
- Добавить обработку ошибок на уровне отдельных методов
- Создать unit-тесты для каждого нового метода
- Рассмотреть возможность использования функционального подхода для некоторых операций


### Анализ логики валидации прогресса (октябрь 2025)

#### Проблема с сохранением частично заполненных данных

**Обнаруженная проблема:** Экран редактирования прогресса не позволял сохранить данные, если хотя бы одно поле было пустым, даже если остальные поля были заполнены корректно.

**Корень проблемы:** В `ProgressService.canSave` логика валидации была слишком строгой - требовала валидности всех полей, включая пустые строки.

#### Анализ поведения в других приложениях

**Android приложение (Android-SOTKA):**
```kotlin
// В модели данных Progress.kt:
fun hasProgress(): Boolean = pullUps != null && pullUps != -1 && 
                           pushUps != null && pushUps != -1 && 
                           squats != null && squats != -1 && 
                           weight != null && weight != -1f

// В ProgressInteractorImpl.saveProgress():
if (progress.hasProgress()) {
    syncInteractor.modifyProgress(progress, NO_PHOTO)
} else {
    syncInteractor.uploadProgress(progress, NO_PHOTO)
}
```

**Анализ логики Android:**
- Метод `hasProgress()` проверяет, что ВСЕ поля заполнены (не null и не -1)
- Прогресс считается полным только при заполнении всех 4 полей
- Синхронизация использует разные методы в зависимости от полноты данных
- Позволяет сохранять частично заполненные данные локально

**Сервер (StreetWorkoutSU):**
```php
// В модели StoProgress.php правила валидации:
[['pullups', 'pushups', 'squats'], 'integer'],
['weight', 'double'],
// Нет ограничений на null значения!

// В ApiStoProgressForm.updateProgress():
$progress->pullups = $this->pullups;    // null значения сохраняются
$progress->pushups = $this->pushups;    // null значения сохраняются
$progress->squats = $this->squats;      // null значения сохраняются
$progress->weight = $this->weight;      // null значения сохраняются
```

**Анализ логики сервера:**
- Сервер принимает и сохраняет null значения для всех полей
- Нет требований к обязательному заполнению всех полей
- Единственное ограничение - уникальность комбинации `user_id` + `day`
- Сервер также принимает значения 0 для всех полей

**Логика валидации в SwiftUI-SotkaApp:**
```swift
// В ProgressService.canSave():
// Разрешаем 0 как допустимое значение (например, если упражнение еще не выполнялось)
let pullUpsValid = isValidNonNegativeInteger(pullUps)  // >= 0
let pushUpsValid = isValidNonNegativeInteger(pushUps)  // >= 0
let squatsValid = isValidNonNegativeInteger(squats)    // >= 0
let weightValid = isValidNonNegativeFloat(weight)      // >= 0
```

**Анализ логики SwiftUI приложения:**
- Принимает частично заполненные данные (как сервер и Android)
- Разрешает нулевые значения (0) как допустимые
- Валидирует только заполненные поля на корректность формата (>= 0)
- Позволяет пользователю сохранять прогресс с любым количеством заполненных полей

#### Выводы анализа

**Корректность текущего поведения:**
✅ **Сервер позволяет частично заполненные данные** - null значения сохраняются корректно
✅ **Android приложение позволяет частично заполненные данные** - сохраняет локально даже неполные данные
❌ **SwiftUI-SotkaApp НЕ позволял частично заполненные данные** - из-за слишком строгой валидации

**✅ Исправления:**
1. ✅ **Исправлена логика валидации в `ProgressService.canSave`**
   - ✅ Теперь валидируются только заполненные поля
   - ✅ Пустые поля считаются валидными (как на сервере и в Android)

2. ✅ **Сохранение корректности:**
   - ✅ Пользователь может сохранить прогресс с любым количеством заполненных полей
   - ✅ Сервер корректно обрабатывает частично заполненные данные
   - ✅ Синхронизация работает независимо от полноты данных

#### Рекомендации для будущего

**✅ Архитектурные принципы:**
- ✅ Принимать частично заполненные данные как валидные (как Android и сервер)
- ✅ Валидировать только заполненные поля на корректность формата
- ✅ Позволять пользователю сохранять прогресс на любом этапе заполнения

**✅ UI/UX улучшения:**
- ✅ Показывать индикаторы заполненности для каждого поля
- ✅ Разрешать сохранение в любой момент (как в Android приложении)
- ✅ Не блокировать сохранение из-за пустых полей

#### ✅ Реализована валидация запятых в поле веса

**Проблема:**
В поле ввода веса (`weightSection`) используется `keyboardType: .decimalPad`, который позволяет вводить запятые, но не ограничивает их количество. Пользователь может ввести несколько запятых подряд (например, "70,,5" или "70,5,5"), что некорректно для числового значения.

**✅ Реализованное решение:**

1. **✅ Расширен `SWTextField` параметром `inputValidation`:**
   ```swift
   public init(
       placeholder: String,
       text: Binding<String>,
       lineLimit: Int = 1,
       isSecure: Bool = false,
       isFocused: Bool,
       errorState: ErrorState? = nil,
       inputValidation: InputValidation? = nil  // Новый параметр
   )
   
   enum InputValidation {
       case decimalNumber  // Для веса: только цифры + максимум одна запятая/точка
       case integer        // Для целых чисел: только цифры
   }
   ```

2. **✅ Реализована логика валидации в реальном времени:**
   ```swift
   private func validateDecimalInput(_ input: String) -> String {
       var result = ""
       var hasDecimalSeparator = false
       
       for character in input {
           if character.isNumber {
               result.append(character)
           } else if (character == "," || character == ".") && !hasDecimalSeparator {
               // Используем запятую как стандартный разделитель
               result.append(",")
               hasDecimalSeparator = true
           }
       }
       
       return result
   }
   ```

3. **✅ Интегрировано в `ProgressInputRow`:**
   ```swift
   SWTextField(
       placeholder: NSLocalizedString("Progress.Edit.Enter", comment: ""),
       text: $value,
       isFocused: focus.wrappedValue == field,
       inputValidation: dataType == .weight ? .decimalNumber : .integer
   )
   ```

4. **✅ Созданы расширения для работы с числами:**
   - `Float.fromUIString()` - парсинг из строки с поддержкой запятой и точки
   - `Float.formattedForUI()` - форматирование для UI с запятой
   - `Int?.stringFromInt()` - преобразование в строку с обработкой 0
   - `Float?.stringFromFloat()` - преобразование в строку с обработкой 0.0

5. **✅ Добавлены валидаторы в `String+`:**
   - `isValidNonNegativeInteger` - валидация неотрицательных целых чисел
   - `isValidNonNegativeFloat` - валидация неотрицательных чисел с плавающей точкой

6. **✅ Созданы comprehensive unit-тесты:**
   - Тесты для всех расширений (`StringExtensionTests`, `FloatExtensionTests`, `IntExtensionTests`)
   - Тесты для `ProgressService` с проверкой валидации
   - Тестирование edge cases (пустая строка, только запятая, множественные запятые)

**Результат:**
- ✅ Пользователь не может ввести множественные запятые в поле веса
- ✅ Автоматическая замена точки на запятую для UI
- ✅ Валидация в реальном времени без блокировки ввода
- ✅ Поддержка как запятой, так и точки как разделителя
- ✅ Обратная совместимость с существующим кодом
- ✅ Comprehensive тестирование всех компонентов

### Интеграция фотографий

- Реализовать интеграцию фотографий прогресса по описанному плану
- Добавить UI компоненты для работы с фотографиями
- Реализовать синхронизацию фотографий с сервером

## Исправление логики доступности прогресса

### Проблема
В новом iOS приложении логика доступности прогресса сдвинута на один день вперед по сравнению с Android и старым iOS приложением:
- **Android/Старое iOS**: средний прогресс доступен с 49 дня программы
- **Новое iOS**: средний прогресс доступен с 50 дня программы ❌

### Архитектурный анализ
- **Контрольные точки прогресса**: день 1, день 49, день 100 (`Progress.Section`)
- **Текущий день программы**: рассчитывается через `StatusManager.currentDayCalculator.currentDay`
- **Маппинг дней**: существует маппинг между внутренними днями приложения (1, 49, 100) и серверными днями (1, 49, 99)

### Варианты реализации

#### Вариант 1: Валидация в самом представлении ProgressGridView

**Описание:**
Добавить параметр `currentDay: Int` в `ProgressGridView` и реализовать логику отключения кнопки внутри представления.

**Преимущества:**
- Простая реализация
- Логика валидации находится рядом с UI
- Легко тестировать

**Недостатки:**
- Зависимость от внешнего параметра
- Логика валидации размазана по UI слою

**Реализация:**
```swift
struct ProgressGridView: View {
    let user: User
    let progressItems: [Progress]
    let currentDay: Int  // Новый параметр
    let onProgressTap: (Progress) -> Void

    // В функции makeModel добавить проверку доступности
    func makeModel(for section: Progress.Section) -> (progress: Progress, isDisabled: Bool) {
        let progress = progressItems.first { $0.id == section.rawValue } ?? .init(id: section.rawValue)
        let isDisabled = currentDay < section.rawValue
        return (progress, isDisabled)
    }
}
```

#### Вариант 2: Computed property в модели Progress

**Описание:**
Добавить computed property в модель `Progress` для проверки доступности кнопки.

**Преимущества:**
- Логика инкапсулирована в модели данных
- Легко переиспользовать
- Четкое разделение ответственности

**Недостатки:**
- Требует передачи текущего дня в модель
- Может нарушать принцип единственной ответственности модели

**Реализация:**
```swift
extension Progress {
    /// Проверяет, доступна ли кнопка для заполнения прогресса
    func isButtonDisabled(currentDay: Int) -> Bool {
        // Прогресс недоступен, если текущий день меньше дня контрольной точки
        return currentDay < id
    }
}
```

#### Вариант 3: Логика валидации в ViewModel экрана

**Описание:**
Добавить логику валидации в ViewModel экрана прогресса и передать результат в представление.

**Преимущества:**
- Централизованная логика валидации
- Легко тестировать бизнес-логику отдельно от UI
- Не загрязняет модель данных UI-логикой

**Недостатки:**
- Требует создания/модификации ViewModel
- Дополнительный слой абстракции

**Реализация:**
```swift
@Observable
final class ProgressViewModel {
    private let statusManager: StatusManager

    var currentDay: Int {
        statusManager.currentDayCalculator?.currentDay ?? 1
    }

    /// Проверяет, доступен ли прогресс для заполнения
    func isProgressAvailable(_ progress: Progress) -> Bool {
        currentDay >= progress.id
    }
}
```

#### Вариант 4: Расширение модели User методом для проверки доступности

**Описание:**
Добавить метод в модель `User` для проверки доступности всех прогрессов для текущего дня.

**Преимущества:**
- Логика связана с пользователем, что логично
- Переиспользуется существующая архитектура
- Не требует дополнительных зависимостей

**Недостатки:**
- Требует передачи текущего дня в метод
- Менее прямолинейно для конкретной кнопки

**Реализация:**
```swift
extension User {
    /// Проверяет, доступен ли прогресс для заполнения в текущий день
    func isProgressAvailable(for progressDay: Int, currentDay: Int) -> Bool {
        currentDay >= progressDay
    }

    /// Возвращает доступные для заполнения секции прогресса
    func availableProgressSections(currentDay: Int) -> [Progress.Section] {
        Progress.Section.allCases.filter { currentDay >= $0.rawValue }
    }
}
```

## План исправления логики прогресса

### Необходимые изменения

#### 1. Обновить модель `Progress.swift`

**Текущая реализация (ошибочная):**
```swift
enum Section: Int, CaseIterable, Codable {
    case one = 1      // День 1
    case two = 50     // День 50 ❌
    case three = 100  // День 100

    init(day: Int) {
        switch day {
        case 1 ... 49: self = .one
        case 50 ... 99: self = .two  // ❌ Должен начинаться с 49
        case 100...: self = .three
        default: self = .one
        }
    }
}
```

**Правильная реализация:**
```swift
enum Section: Int, CaseIterable, Codable {
    case one = 1      // День 1
    case two = 49     // День 49 ✅
    case three = 100  // День 100

    init(day: Int) {
        switch day {
        case 1 ... 48: self = .one      // Базовый блок
        case 49 ... 99: self = .two     // Продвинутый блок ✅
        case 100...: self = .three     // Заключение
        default: self = .one
        }
    }
}
```

**Обновить маппинг функций:**
```swift
static func getExternalDayFromProgressId(_ internalId: Int) -> Int {
    switch internalId {
    case 1: 1      // День 1 соответствует серверному дню 1
    case 49: 49    // День 49 соответствует серверному дню 49 ✅
    case 100: 99   // День 100 соответствует серверному дню 99
    default: internalId
    }
}

static func getInternalDayFromExternalDay(_ externalDay: Int) -> Int {
    switch externalDay {
    case 1: 1      // Серверный день 1 соответствует внутреннему дню 1
    case 49: 49    // Серверный день 49 соответствует внутреннему дню 49 ✅
    case 99: 100   // Серверный день 99 соответствует внутреннему дню 100
    default: externalDay
    }
}
```

#### 2. Обновить документацию и комментарии
- Изменить все ссылки на "день 50" на "день 49" в документации
- Обновить комментарии в коде
- Обновить описания в планах реализации

#### 3. Обновить тесты и превью
- Изменить тестовые данные для соответствия новым дням (день 49 вместо 50)
- Обновить превью в `ProgressGridView` для демонстрации правильной логики:
  - Превью "Доступность кнопок - день 25 (день 49 недоступен)" ✅ (уже обновлено)
  - Превью "Доступность кнопок - день 75 (день 100 недоступен)" оставить как есть
- Обновить комментарии в тестах с правильными днями

#### 4. Проверить интеграцию
- Убедиться, что синхронизация работает с новыми днями
- Проверить, что серверные запросы используют правильный маппинг
- Тестировать UI на разных днях программы:
  - День 48: только день 1 доступен
  - День 49: дни 1 и 49 доступны (средний прогресс)
  - День 50+: дни 1, 49 и 100 доступны (продвинутый блок)
  - День 100+: все дни доступны

### Целевые дни доступности прогресса
После исправления:
- **День 1**: доступен всегда (currentDay >= 1)
- **День 49**: доступен с 49 дня программы (currentDay >= 49) - средний прогресс
- **День 100**: доступен с 100 дня программы (currentDay >= 100) - финальный прогресс

**Результат исправления:** Теперь логика доступности прогресса в новом iOS приложении соответствует Android и старому iOS приложению.

### Влияние на структуру программы
После исправления структура программы будет следующей:
- **Базовый блок**: дни 1-48 (вместо 1-49)
- **Продвинутый блок**: дни 49-99 (вместо 50-99)
- **Заключение**: дни 100+ (остается без изменений)

Это соответствует структуре, описанной в документации программы и логике Android/старого iOS приложений.

### Дополнительные улучшения

- Добавить метрики для отслеживания эффективности синхронизации
- Рассмотреть возможность добавления retry-механизма для неуспешных синхронизаций
- Оптимизировать производительность синхронизации для больших объемов данных

---

## ✅ Выполненные задачи

### ✅ Реализация отключения кнопок прогресса

**Реализовано:**
1. **✅ Добавлен параметр `currentDay`** в `ProgressGridView`
2. **✅ Изменена логика `makeModel`** для возврата кортежа `(progress, isDisabled)`
3. **✅ Добавлен `.disabled` модификатор** к кнопкам прогресса
4. **✅ Передан текущий день** из `ProgressScreen` через `StatusManager`

**Результат:** Кнопки прогресса теперь корректно отключаются в зависимости от текущего дня программы пользователя.

### ✅ Создание заглушки StatusManager для превью

**Реализовано:**
1. **✅ Создан файл `StatusManager+.swift`** в `PreviewContent`
2. **✅ Добавлено статичное свойство `preview`** для переиспользования в тестах
3. **✅ Обновлены все превью** для использования заглушки вместо дублирования кода

**Результат:** Упрощена поддержка кода и снижена вероятность ошибок в превью.

### ✅ Валидация ввода в реальном времени

**Реализовано:**
1. **✅ Расширение `SWTextField`** с параметром `inputValidation`
   - Добавлен enum `InputValidation` с типами `.decimalNumber` и `.integer`
   - Реализована валидация в реальном времени через `onChange`
   - Поддержка как запятой, так и точки как разделителя для десятичных чисел

2. **✅ Создание расширений для работы с числами:**
   - `Double+.swift` - форматирование и парсинг Double
   - `Float+.swift` - форматирование и парсинг Float с поддержкой UI строк
   - `Int+.swift` - преобразование Int в строки для UI
   - `String+.swift` - валидаторы для неотрицательных чисел

3. **✅ Comprehensive unit-тесты:**
   - `StringExtensionTests.swift` - 20 тестов для валидации строк
   - `FloatExtensionTests.swift` - 25 тестов для работы с Float
   - `IntExtensionTests.swift` - 8 тестов для работы с Int
   - `ProgressServiceTests.swift` - 25 тестов для сервиса прогресса

4. **✅ Интеграция в UI:**
   - Обновлен `EditProgressScreen` для использования валидации
   - Поле веса использует `.decimalNumber` валидацию
   - Поля упражнений используют `.integer` валидацию

### ✅ Улучшение логики валидации прогресса

**Реализовано:**
1. **✅ Исправлена логика `ProgressService.canSave`:**
   - Теперь валидируются только заполненные поля
   - Пустые поля считаются валидными (как на сервере и в Android)
   - Разрешены нулевые значения (0) как допустимые

2. **✅ Улучшена обработка данных:**
   - Автоматическое преобразование 0 в пустые строки для UI
   - Корректная обработка nil значений
   - Поддержка частично заполненных данных

3. **✅ Comprehensive тестирование:**
   - Тесты для всех сценариев валидации
   - Тесты для edge cases (пустые строки, нулевые значения, отрицательные числа)
   - Тесты для интеграции с UI

### ✅ Упрощение и рефакторинг синхронизации

**Реализовано:**
1. **✅ Упрощена логика `ProgressSyncService`:**
   - Убрана двойная попытка отправки (upload + modify)
   - Используется единый `updateProgress` для всех случаев
   - Сервер сам применяет LWW логику по датам модификации

2. **✅ Улучшена обработка ошибок:**
   - Убран хардкод проверки текста ошибки
   - Используется универсальная логика на основе кодов ответа HTTP
   - Упрощена обработка дубликатов

3. **✅ Оптимизированы методы:**
   - Упрощена логика в `downloadServerProgress`
   - Улучшена читаемость `cleanupDuplicateProgress`
   - Добавлено подробное логирование на русском языке

### ✅ Результаты

**Достигнуто:**
- ✅ Пользователь не может ввести некорректные данные в поля ввода
- ✅ Валидация происходит в реальном времени без блокировки UI
- ✅ Поддержка частично заполненных данных (как в Android приложении)
- ✅ Автоматическое форматирование чисел для UI (запятая вместо точки)
- ✅ Comprehensive тестирование всех компонентов
- ✅ Упрощенная и надежная логика синхронизации
- ✅ Улучшенная обработка ошибок и дубликатов

**Технические улучшения:**
- ✅ Расширена дизайн-система `SWTextField` с валидацией
- ✅ Создана библиотека утилит для работы с числами
- ✅ Добавлено 78 unit-тестов для всех компонентов
- ✅ Улучшена архитектура синхронизации
- ✅ Добавлено подробное логирование на русском языке

## Интеграция фотографий прогресса

### Краткий обзор функционала

- **Типы фотографий**: фронтальная, сзади, сбоку для каждой контрольной точки (день 1, 49, 100)
- **Хранение**: локальные бинарные данные в SwiftData + URL для синхронизации
- **UI компоненты**: `ProgressPhotoView`, `ProgressPhotoPicker`, `ProgressPhotoGrid`
- **Обработка изображений**: `ImageProcessor` для масштабирования и сжатия
- **Синхронизация**: multipart/form-data загрузка через `ProgressClient`

### Модель данных: `ProgressPhoto`

- `type: PhotoType` — тип фотографии (фронтальная, сзади, сбоку)
- `data: Data?` — локальные бинарные данные фотографии
- `urlString: String?` — URL фотографии с сервера
- `isSynced: Bool` — синхронизировано ли состояние с сервером
- `isDeleted: Bool` — пометка на удаление (soft delete)

### Расширение модели `Progress`

- Добавление массива `photos: [ProgressPhoto]` для хранения фотографий
- Методы для работы с фотографиями: `getPhoto()`, `setPhoto()`, `deletePhoto()`
- Computed properties: `hasPhotos`, `hasUnsyncedPhotos`, `hasPhotosToDelete`

### Сервисы

- **ImageProcessor**: обработка изображений (масштабирование, сжатие, валидация)
- **ProgressService**: расширение методами работы с фотографиями
- **ProgressSyncService**: синхронизация фотографий в фоне

### Сетевая интеграция

- **ProgressClient**: методы для загрузки и удаления фотографий
- **ProgressRequest**: модель для отправки данных + фотографий
- **multipart/form-data**: загрузка изображений через `BodyMaker`

### UI компоненты

- **ProgressPhotoView**: отображение фотографии с placeholder'ом
- **ProgressPhotoPicker**: выбор фотографий из галереи или камеры
- **ProgressPhotoGrid**: сетка из 3 фотографий для каждой контрольной точки

### Офлайн-приоритет

- Все операции с фотографиями сначала сохраняются локально
- Синхронизация выполняется асинхронно в фоне
- При сетевых ошибках работа продолжается локально
- Конфликты разрешаются по стратегии LWW

### Ключевые правила для фотографий прогресса

- Не блокировать UI ожиданием сети при работе с фотографиями.
- Всегда писать логи на русском с достаточным контекстом.
- Обеспечивать офлайн-приоритет: локальное хранение в первую очередь.
- Синхронизировать фотографии асинхронно и неблокирующе.
- Разрешать конфликты автоматически по `lastModified`.
- Поддерживать мягкое удаление (soft delete) для фотографий.